# Dependency pins live in pyproject.toml (single source of truth). This file
# installs kgsynth itself, editable, with its test extras — so the historical
# `pip install -r requirements.txt` entry point keeps working.
--editable .[test]
